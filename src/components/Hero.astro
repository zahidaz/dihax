---
---

<section class="hero">
  <div class="hero-container">
    <div class="hero-left">
      <h1 class="slogan">
        <span class="slogan-line accent-blue">IT&nbsp;Support</span>
        <span class="slogan-line">That Empowers,</span>
        <span class="slogan-line accent-red space-top">Cybersecurity</span>
        <span class="slogan-line">That Protects!</span>
      </h1>
    </div>
    <div class="hero-right">
      <p class="hero-tagline">Expert IT support and cybersecurity solutions tailored to your needs.</p>
      <div class="cta-buttons">
        <a href="#services" class="btn btn-secondary">IT Services</a>
        <a href="#services" class="btn btn-primary">Security Services</a>
      </div>
    </div>
  </div>
  <div class="glass-card">
    <div class="card-content">
      <h2>Expert Team, Proven Experience</h2>
      <p>Specialized in comprehensive IT infrastructure support, mobile & web security, and penetration testing for clients worldwide.</p>
      <div class="stats">
        <div class="stat">
          <span class="stat-number">10+</span>
          <span class="stat-label">Years Combined Experience</span>
        </div>
        <div class="stat">
          <span class="stat-number">Est. 2025</span>
          <span class="stat-label">Founded in BC</span>
        </div>
        <div class="stat">
          <span class="stat-number">Local</span>
          <span class="stat-label">British Columbia Based</span>
        </div>
      </div>
      <div class="info-grid">
        <div class="info-item">
          <svg class="info-icon" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
            <path d="M21 10c0 7-9 13-9 13s-9-6-9-13a9 9 0 0 1 18 0z"/>
            <circle cx="12" cy="10" r="3"/>
          </svg>
          <span>British Columbia, Canada</span>
        </div>
        <div class="info-item">
          <svg class="info-icon" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
            <polyline points="22 12 18 12 15 21 9 3 6 12 2 12"/>
          </svg>
          <span>Fast Response Time</span>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  .hero {
    min-height: 100vh;
    display: flex;
    flex-direction: column;
    padding: 140px 20px 0;
    position: relative;
  }

  .hero-container {
    width: 100%;
    max-width: 1200px;
    margin: 0 auto;
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 60px;
    align-items: center;
    margin-bottom: 60px;
  }


  .hero-left .slogan {
    font-size: 42px;
    font-weight: 400;
    line-height: 1.3;
    margin: 0;
    font-family: 'SF Mono', 'Monaco', 'Consolas', monospace;
    display: flex;
    flex-direction: column;
    animation: fadeInUp 0.8s ease-out;
  }

  @keyframes fadeInUp {
    from {
      opacity: 0;
      transform: translateY(20px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  .slogan-line {
    display: block;
    color: #000;
    font-weight: 400;
    margin-bottom: 4px;
    opacity: 0;
    animation: fadeIn 0.6s ease-out forwards;
  }

  .slogan-line:nth-child(1) {
    animation-delay: 0.2s;
  }

  .slogan-line:nth-child(2) {
    animation-delay: 0.4s;
  }

  .slogan-line:nth-child(3) {
    animation-delay: 0.6s;
  }

  .slogan-line:nth-child(4) {
    animation-delay: 0.8s;
  }

  @keyframes fadeIn {
    to {
      opacity: 1;
    }
  }

  .slogan-line.accent-red {
    color: #ff5757;
    font-weight: 500;
  }

  .slogan-line.accent-blue {
    color: #0EA5E9;
    font-weight: 500;
  }

  .slogan-line.space-top {
    margin-top: 24px;
  }

  .hero-right {
    display: flex;
    flex-direction: column;
    justify-content: flex-end;
    align-items: center;
    gap: 24px;
  }

  .hero-tagline {
    font-size: 15px;
    color: #545454;
    line-height: 1.6;
    margin: 0;
    font-family: system-ui, -apple-system, sans-serif;
    font-weight: 400;
    animation: fadeIn 0.6s ease-out 1s backwards;
    text-align: center;
  }

  .cta-buttons {
    display: flex;
    gap: 16px;
  }

  .btn {
    padding: 16px 32px;
    border-radius: 12px;
    text-decoration: none;
    font-weight: 400;
    font-size: 16px;
    font-family: 'SF Mono', 'Monaco', 'Consolas', monospace;
    transition: all 0.3s ease;
    display: inline-block;
    border: 1px solid rgba(255, 255, 255, 0.3);
    backdrop-filter: blur(10px);
    position: relative;
    overflow: hidden;
  }

  .btn::before {
    content: '';
    position: absolute;
    top: 50%;
    left: 50%;
    width: 0;
    height: 0;
    border-radius: 50%;
    background: rgba(255, 255, 255, 0.3);
    transform: translate(-50%, -50%);
    transition: width 0.6s, height 0.6s;
  }

  .btn:active::before {
    width: 300px;
    height: 300px;
  }

  .btn-primary {
    background: rgba(255, 87, 87, 0.15);
    color: #ff5757;
    box-shadow: 0 4px 12px rgba(255, 87, 87, 0.2);
  }

  .btn-primary:hover {
    background: rgba(255, 87, 87, 0.25);
    box-shadow: 0 6px 16px rgba(255, 87, 87, 0.3);
    transform: translateY(-2px);
  }

  .btn-secondary {
    background: rgba(14, 165, 233, 0.15);
    color: #0EA5E9;
    box-shadow: 0 4px 12px rgba(14, 165, 233, 0.2);
  }

  .btn-secondary:hover {
    background: rgba(14, 165, 233, 0.25);
    box-shadow: 0 6px 16px rgba(14, 165, 233, 0.3);
    transform: translateY(-2px);
  }

  @media (max-width: 768px) {
    .hero {
      padding: 90px 20px 0;
    }

    .hero-container {
      grid-template-columns: 1fr;
      gap: 40px;
      margin-bottom: 60px;
      text-align: center;
    }

    .hero-left .slogan {
      font-size: 28px;
      align-items: center;
    }

    .hero-right {
      justify-content: center;
      align-items: center;
    }

    .hero-tagline {
      max-width: 300px;
    }

    .hero-image {
      min-height: 150px;
      border-radius: 30px 30px 0 0;
      max-width: 100%;
    }

    .slogan-line.space-top {
      margin-top: 16px;
    }

    .cta-buttons {
      flex-direction: column;
      width: 100%;
      align-items: center;
    }

    .btn {
      text-align: center;
      width: 100%;
      max-width: 300px;
    }
  }

  @media (max-width: 480px) {
    .hero {
      padding-top: 70px;
    }

    .hero-left .slogan {
      font-size: 24px;
    }

    .btn {
      font-size: 14px;
      padding: 14px 24px;
    }
  }

  .glass-card {
    width: 100%;
    max-width: 1200px;
    margin: 0 auto 40px;
    padding: 60px 40px;
    background: linear-gradient(135deg, rgba(255, 87, 87, 0.05) 0%, rgba(14, 165, 233, 0.05) 100%);
    backdrop-filter: blur(20px);
    border: 1px solid rgba(255, 255, 255, 0.3);
    border-radius: 24px;
    box-shadow: 0 8px 32px rgba(0, 0, 0, 0.1);
    animation: fadeIn 0.6s ease-out 1.2s backwards;
    position: relative;
    overflow: hidden;
  }

  .glass-card::before {
    content: '';
    position: absolute;
    top: -50%;
    right: -10%;
    width: 400px;
    height: 400px;
    background: radial-gradient(circle, rgba(255, 87, 87, 0.1) 0%, transparent 70%);
    border-radius: 50%;
    pointer-events: none;
  }

  .glass-card::after {
    content: '';
    position: absolute;
    bottom: -50%;
    left: -10%;
    width: 400px;
    height: 400px;
    background: radial-gradient(circle, rgba(14, 165, 233, 0.1) 0%, transparent 70%);
    border-radius: 50%;
    pointer-events: none;
  }

  .card-content {
    position: relative;
    z-index: 1;
    text-align: center;
  }

  .card-content h2 {
    font-size: 32px;
    font-weight: 500;
    margin: 0 0 16px;
    color: #000;
    font-family: system-ui, -apple-system, sans-serif;
  }

  .card-content p {
    margin: 0 0 40px;
    color: #545454;
    font-size: 16px;
    line-height: 1.6;
    font-family: system-ui, -apple-system, sans-serif;
  }

  .stats {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 40px;
  }

  .stat {
    display: flex;
    flex-direction: column;
    gap: 8px;
  }

  .stat-number {
    font-size: 36px;
    font-weight: 600;
    background: linear-gradient(135deg, #ff5757 0%, #0EA5E9 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    font-family: 'SF Mono', 'Monaco', 'Consolas', monospace;
  }

  .stat-label {
    font-size: 14px;
    color: #545454;
    font-family: system-ui, -apple-system, sans-serif;
  }

  .info-grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 20px;
    margin-top: 32px;
    padding-top: 32px;
    border-top: 1px solid rgba(0, 0, 0, 0.1);
  }

  .info-item {
    display: flex;
    align-items: center;
    gap: 12px;
    justify-content: center;
  }

  .info-item .info-icon {
    width: 20px;
    height: 20px;
    color: #545454;
  }

  .info-item span {
    font-size: 14px;
    color: #545454;
    font-family: system-ui, -apple-system, sans-serif;
  }

  @media (max-width: 768px) {
    .glass-card {
      padding: 40px 28px;
      margin-bottom: 20px;
    }

    .card-content h2 {
      font-size: 24px;
    }

    .card-content p {
      font-size: 14px;
      margin-bottom: 32px;
    }

    .stats {
      grid-template-columns: 1fr;
      gap: 24px;
    }

    .stat-number {
      font-size: 28px;
    }

    .stat-label {
      font-size: 13px;
    }

    .info-grid {
      grid-template-columns: 1fr;
      gap: 16px;
    }
  }

  @media (max-width: 480px) {
    .glass-card {
      padding: 32px 20px;
    }

    .card-content h2 {
      font-size: 20px;
    }

    .stat-number {
      font-size: 24px;
    }
  }
</style>
